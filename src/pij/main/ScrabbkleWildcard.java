package pij.main;

public class ScrabbkleWildcard { //implements Tile, but would be different type for board
    //should EXTEND ScrabbleTile instead so can use with Square
    //the value of a wildcard in ScraBBKle is 3.
    //wildcards are entered as lower case when making a move, e.g. 'BLuE' where 'u' is the wildcard
}
